<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_editor"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.flawiddsouza.POSTer.EditorActivity">

    <EditText
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top"
        android:hint="@string/editor_name_hint" />
    <EditText
        android:id="@+id/url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top"
        android:hint="@string/editor_url_hint"
        android:inputType="textWebEmailAddress" />

    <EditText
        android:id="@+id/parameter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="top"
        android:hint="@string/editor_parameter_hint" />

    <CheckBox
        android:id="@+id/appendHeading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:checked="true"
        android:hint="@string/editor_append_heading_hint" />

    <EditText
        android:id="@+id/staticParameters"
        android:layout_width="match_parent"
        android:layout_height="194dp"
        android:gravity="top"
        android:hint="@string/editor_static_parameters" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="You can use {{heading}} in static parameters to substitute it with the appended heading when sharing" />
</LinearLayout>